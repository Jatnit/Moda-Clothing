<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moda Studio | 404</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/checkout.css">
</head>

<body class="<%= theme === 'dark' ? 'theme-dark' : 'theme-light' %>">
    <%- include('../partials/header') %>
        <main class="site-main">
            <section class="page-404">
                <div class="page-404__shell">
                    <div class="page-404__marquee page-404__marquee--top">
                        <div class="page-404__marquee-inner">
                            <% for (let i=0; i < 6; i +=1) { %>
                                <div class="page-404__marquee-text">// <span>The page is in production</span> // 404
                                    error
                                </div>
                                <% } %>
                        </div>
                        <div class="page-404__marquee-inner">
                            <% for (let i=0; i < 6; i +=1) { %>
                                <div class="page-404__marquee-text">// <span>The page is in production</span> // 404
                                    error
                                </div>
                                <% } %>
                        </div>
                    </div>

                    <div class="page-404__content">
                        <p class="page-404__label mb-2">Nite Riot Inspired</p>
                        <p class="page-404__subtitle mb-1">The page is in production</p>
                        <h1 class="page-404__title">404</h1>
                        <div class="page-404__divider"></div>
                        <p class="lead mb-4">
                            Trang bạn đang tìm không tồn tại hoặc đã được chuyển đi. Hãy quay lại trang chủ để tiếp tục
                            hành
                            trình cùng Moda Studio.
                        </p>
                        <div class="d-flex flex-column flex-sm-row gap-3 justify-content-center">
                            <a href="/" class="btn btn-light btn-lg page-404__cta text-uppercase">Return Home</a>
                            <a href="/products" class="btn btn-outline-light btn-lg page-404__cta text-uppercase">Xem
                                sản
                                phẩm</a>
                        </div>
                    </div>

                    <div class="page-404__marquee page-404__marquee--bottom page-404__marquee--reverse">
                        <div class="page-404__marquee-inner">
                            <% for (let i=0; i < 6; i +=1) { %>
                                <div class="page-404__marquee-text">// <span>The page is in production</span> // 404
                                    error
                                </div>
                                <% } %>
                        </div>
                        <div class="page-404__marquee-inner">
                            <% for (let i=0; i < 6; i +=1) { %>
                                <div class="page-404__marquee-text">// <span>The page is in production</span> // 404
                                    error
                                </div>
                                <% } %>
                        </div>
                    </div>
                </div>
            </section>
        </main>
        <%- include('../partials/footer') %>

            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>